# QA 报告（qa-report.md）

## 范围
app/showcase 从单页海报改造为 9 页 PPT 式幻灯片（方向 B 全幅沉浸）：8 页叙事 + 1 页结尾（原海报），2026-08-22 完成。

## 叙事调整（按用户要求）
1. 第 1 页浓缩为要点（去掉第一人称，起提词作用，不再展示原文）。
2. 第 2 页只保留「没有目标，没有数值，没有评判」。
3. 原 4、5 页合并为一页。
4. 原第 8 页改为「幸福是什么？」。
5. 原第 9 页删除。
6. 原第 10 页去掉开头的「而」→「这便是findjoy的意义所在。」。
保留的原文逐字不改（如「ai驱动」小写、标点原样）。

## 验证结果
- `npm run typecheck` ✅ / `npm run lint` ✅ / `npm test` ✅（83 用例）/ `npm run build` ✅（/showcase 静态预渲染）

## 浏览器 QA（Playwright，Chromium 151）
**80/80 检查通过**，覆盖 4 断点：1920×1080、1366×768、390×844、280×653。

### 逐页断言
- 第 1 页：主句「一个 idea」+ 4 条要点齐全；渲染文本无第一人称「我」。
- 第 2 页：仅「没有目标，没有数值，没有评判」。
- 第 4 页：原 4+5 两段合并同页，逐字一致。
- 第 7 页：幸福是什么？；第 8 页：这便是findjoy的意义所在。（无「而」）。
- 结尾页（9）：原海报 + 二维码加载正常。
- 计数器 01/09 → 04 → 07 → 08 → 09 全部同步；Home 回第 1 页。
- 每页无横向溢出、无页面滚动、无元素溢出视口；配图全部加载。

## 截图（final-screenshots/）
- venue / laptop / mobile / tiny 四断点 × {slide1, slide4, slide7, slide8, slide9-ending}

## 已知限制
- 配图 1920px 宽，4K 大屏略软。
- 触屏滑动经代码审查确认，未自动化模拟。
- 结尾页 ≤480px 隐藏三支柱与金句（移动端以二维码与转化优先）。
