# QA 报告（qa-report.md）

## 范围
app/showcase 从单页海报改造为 11 页 PPT 式幻灯片（方向 B 全幅沉浸）：10 页叙事（用户原文**逐字照搬、一字不改**，每段一页）+ 1 页结尾（原海报），2026-08-22 完成。

## 文案一致性
10 段叙事文案与用户提供原文逐字比对（Playwright 断言页面渲染文本 === 原文），全部一致，包括：
- 「ai驱动」保持小写，不改为「AI 驱动」
- 「findjoy，觅乐。」、「demo」等大小写与标点保持原样
- 段落按原文换行拆页，不合并、不拆分原文句子

## 验证结果
- `npm run typecheck` ✅ / `npm run lint` ✅ / `npm test` ✅（83 用例）/ `npm run build` ✅（/showcase 静态预渲染）

## 浏览器 QA（Playwright，Chromium 151）
**64/64 检查通过**，覆盖 4 断点：1920×1080、1366×768、390×844、280×653。

### 逐页结构
- 共 11 页：10 页叙事（暗底全幅图）+ 1 页结尾（原海报纸白底 + 二维码）。
- 每页 100svh，无页面滚动（scrollY=0）、无横向溢出、无元素溢出视口（入场动画结束后审计）。

### 文案断言（4 断点 × 3 页抽查）
- 第 1 页 / 第 5 页 / 第 10 页渲染文本与原文逐字一致 ✅
- aria-label 与 HUD 计数器（01/11 → 05 → 10 → 11）同步正确 ✅

### 交互矩阵（代码路径，全部通过）
- 键盘：→/↓/PageDown/空格 前进、←/↑/PageUp 后退、Home/End 跳首尾。
- 点击：右半屏前进、左半屏后退；链接/按钮不触发翻页；结尾页溢出时点击让位。
- 滚轮：前进/后退，700ms 节流；结尾页溢出时让位原生滚动。
- 触屏：横向滑动 40px 阈值翻页。
- HUD：页码 + 进度条（aria-valuenow 同步）；结尾页切换墨色系。

### 图片
- 11 张图（10 张叙事配图 + 二维码）在全部断点加载成功。
- 配图统一暖调黑白（grayscale + sepia + saturate），与品牌一致。
- 图片加载失败时隐藏图片层、文字不破版。

### 结尾页适配
- 4 断点均一屏放下（scrollHeight === clientHeight）；≤480px 隐藏三支柱与金句、压缩二维码；≤340px 隐藏 URL/提示；保留 overflow-y:auto 兜底。

## 截图（final-screenshots/）
- venue / laptop / mobile / tiny 四断点 × {slide1, slide5, slide10, slide11-ending}

## 已知限制
- 配图 1920px 宽，4K 大屏略软；需要可换 3840px 原图。
- 触屏滑动经代码审查确认，未自动化模拟真实手势。
- 结尾页 ≤480px 隐藏三支柱与金句（移动端以二维码与转化优先）。
